name: yuv_converter
description: Flutter yuv convert to rgba8888
version: 0.0.1
homepage: https://github.com/menshen/flutter_yuv_converter

environment:
  sdk: '>=3.0.6 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
flutter:
